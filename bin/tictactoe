#!/usr/bin/env ruby

require_relative '../config/environment'

# * Greet the user with a message.
# * Prompt the user for what kind of game they want to play, 0,1, or 2 player.
# * Ask the user for who should go first and be "X".
# * Use the input to correctly initialize a `Game` with the appropriate player types and token values.
# * When the game is over, the CLI should prompt the user if they would like to play
# again and allow them to choose a new configuration for the game as described above.
# If the user doesn't want to play again, exit the program.

puts "Welcome to Tic-Tac-Toe!"
puts "Please enter '0', '1', or '2' player for the type of game you would like to play."

# puts input = gets.strip
input = gets.strip
# if input == 0
#   Game.new().initialize(board = Board.new())
# elsif input == 1
#   Game.new().initialize(player_1 = Players::Human.new("X"), board = Board.new())
# elsif input == 2
#   Game.new().initialize(player_1 = Players::Human.new("X"), player_2 = Players::Human.new("O"), board = Board.new())
if input == 2
   game = Game.new()
   game.initialize(player_1 = Players::Human.new("X"), player_2 = Players::Human.new("O"), board = Board.new())
   puts "Please enter '1' or '2' to choose which player will go first as X."
   input = gets.strip
end



if game.over? == true
  puts "If you would like to play again, please enter '0', '1', or '2' player for the type of game you would like to play."

puts "To quit, type 'exit'."
end
until input == ("exit")
end
