#!/usr/bin/env ruby

require_relative '../config/environment'


play_again = 'y'
while play_again == 'y'
    Game.new.start
    puts "If you would like to play again, enter y."
    puts "If not, enter any other key."
    play_again = gets.chomp
end
