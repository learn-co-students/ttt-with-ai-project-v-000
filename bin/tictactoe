#!/usr/bin/env ruby

require_relative '../config/environment'

# board = Board.new
# player = Human.new("X")
# player = Human.new("O")
# computer_player = Computer.new("O")
# computer_player = Computer.new("X")
while user_input = gets.chomp do
  puts "Greetings user, It's time to play some TIC TAC TOE!"
  puts "Would you wish to watch computer vs computer game?, enter 'watch computer'."
  puts "Would you like to play against the computer?, enter 'play computer'."
  puts "Would you like to play against another player? enter 'play player'."
  puts "press 'exit' to leave."
  puts "What would you like to do?"

  if user_input == 'exit'
    break
  elsif user_input == 'watch computer'
    puts "Who's 'X' computer 1 or computer 2?,enter '1' or '2'."
    user_choice = gets.chomp
    watch_computer(user_token)
  elsif condition
  end
end

def watch_computer(user_choice)
  user_choice = user_choice.to_i

  board = Board.new
  if user_choice == 1
    computer_player_1 = Computer.new("X") && computer_player_2 = Computer.new("O")
  else
    computer_player_2 = Computer.new("X") && computer_player_1 = Computer.new("O")
  end

  game = Game.new(computer_player_1,computer_player_2,board)

end














# game = Game.new(player,computer_player,board)
