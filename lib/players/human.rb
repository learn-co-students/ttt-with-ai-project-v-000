

class Players

    class Human < Player

        def move(board)
            input = gets
            # board.update(input, self.token)
            # board.turn_count.to_s
            input
        end
    end

end
