require 'pry'
module Players

  class Computer < Player

    def move(board)
      "1"

    end

    #
    # valid_move?(input)
    #   index = to_index(user_input)
    #   index>=0 && index<=8 && @cells[index] == " " ? true : false
    # end

  end
end
